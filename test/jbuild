(jbuild_version 1)

(executable
 ((name         tests)
  (public_name  tests)
  (libraries    (core
                 qcheck
                 nomad))
  (preprocess (pps (ppx_deriving.show)))))

(alias
 ((name runtest)
  (deps (tests.exe))
  (action (run ${<}))))

